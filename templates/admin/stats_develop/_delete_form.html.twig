<form method="post" action="{{ path('stats_develop_delete', {'id': stats_develop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stats_develop.id) }}">
    <button class="btn">Delete</button>
</form>
