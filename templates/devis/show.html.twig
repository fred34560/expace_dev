{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>
            <tr>
                <th>Client</th>
                <td>{{ devi.client ? devi.client|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Projet</th>
                <td>{{ devi.projet }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ devi.statut }}</td>
            </tr>
            <tr>
                <th>Services</th>
                <td>{{ devi.services ? devi.services|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ devi.createdAt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('devis_index') }}">back to list</a>

    <a href="{{ path('devis_edit', {'id': devi.id}) }}">edit</a>

    {{ include('devis/_delete_form.html.twig') }}
{% endblock %}
